---
"mdl": major
---

Repository and module path moved from `github.com/sekiju/mdl` to `github.com/knst0/mdl`.

Replaced `sekiju/htt` with `resty.dev/v3` for the HTTP client and `goccy/go-json` with `bytedance/sonic` for JSON handling. `go.mod` now targets Go 1.25.
